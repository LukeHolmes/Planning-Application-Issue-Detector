# Copy to .streamlit/secrets.toml for local overrides (do not commit secrets.toml).
# On Streamlit Community Cloud, add these under App settings → Secrets.

# Optional: path to full dataset inside the repo or mount
# PLANNING_DATA_PATH = "data/sample/planning_applications_sample.csv"
