# Data

## Full dataset (not in git)

Place `Tableau_Ready_Planning_Applications_With_Street_Town_Cleaned.xlsx` in the repository root or set:

```bash
export PLANNING_DATA_PATH=/path/to/your/file.xlsx
```

## Sample data

`data/sample/planning_applications_sample.csv` is a small synthetic dataset used by tests and CI.
